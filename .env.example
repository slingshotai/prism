# Copy to .env (gitignored) OR put these in ~/.config/slingshot/secrets.env
# (loaded automatically, kept outside the repo — preferred so keys never sit
# inside the skill). A real shell export also works.
ASSEMBLYAI_API_KEY=  # required — transcription (assemblyai.com)
OPENAI_API_KEY=      # optional — Whisper API only
